% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trs_pv.R
\name{trs_pv}
\alias{trs_pv}
\title{Computes statistics to describe parallel variations between two rwl-style data frames}
\usage{
trs_pv(x, y = NULL, min_overlap = 50, prob = TRUE, as_df = FALSE)
}
\arguments{
\item{x}{A data frame of test tree-ring series (\code{rwl} format: years in rows, series in columns).}

\item{y}{A data frame of reference series (\code{rwl} format).}

\item{min_overlap}{Integer. Minimum number of overlapping years required for computing GLK/SGC statistics. Default is 50.}

\item{prob}{Logical. If TRUE (default), computes two-tailed p-values for GLK and SGC.}

\item{as_df}{Logical. If TRUE, returns a long-format data.frame with all combinations and results. Default is FALSE.}
}
\value{
If \code{as_df = FALSE}, a list with components:
\describe{
\item{glk}{Matrix of Gleichläufigkeit values.}
\item{glk_p}{Matrix of GLK p-values (if \code{prob = TRUE}).}
\item{sgc}{Matrix of synchronous growth change values.}
\item{ssgc}{Matrix of semi-synchronous growth change values.}
\item{sgc_p}{Matrix of SGC p-values (if \code{prob = TRUE}).}
\item{overlap}{Matrix of the number of overlapping years used in each comparison.}
}

If \code{as_df = TRUE}, returns a data frame with columns:
\code{series}, \code{reference}, \code{glk}, \code{glk_p}, \code{sgc}, \code{ssgc}, \code{sgc_p}, and \code{overlap}.
}
\description{
Compares tree-ring series in two data frames using Gleichläufigkeit (GLK) and
Synchronous Growth Changes (SGC), adapted from the \code{dplR} package.
}
\examples{
# Create sample data,
trs <- trs_pseudo_rwl(n_series = 5, series_length = c(80, 100), end_date = c(2010, 2020))
trs_pv(trs)$sgc

}
