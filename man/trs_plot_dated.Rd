% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trs_plot_dated.R
\name{trs_plot_dated}
\alias{trs_plot_dated}
\title{Plot a dated tree-ring series with a reference chronology}
\usage{
trs_plot_dated(
  x,
  y,
  end_year = NULL,
  zscore = TRUE,
  pv_highlight = TRUE,
  pv_alpha = 0.2,
  show_stats = TRUE
)
}
\arguments{
\item{x}{A \code{data.frame} (typically of class \code{'rwl'}) with one column of ring-width values.
Row names must be numeric and represent calendar years.}

\item{y}{A \code{data.frame} (typically of class \code{'rwl'}) with one column of ring-width values.
Row names must be numeric and represent calendar years.}

\item{end_year}{Optional numeric. If provided, the rownames of \code{x} will be set to match this year for the last ring (using \code{trs_end_date()}).}

\item{zscore}{Logical. If \code{TRUE}, the ring-width series are standardized to z-scores before plotting.
Defaults to \code{TRUE}.}

\item{pv_highlight}{Logical. If \code{TRUE}, highlights regions of parallel variation (synchronous growth change - sgc) using shaded bars.
Defaults to \code{TRUE}.}

\item{pv_alpha}{Numeric between 0 and 1. Controls the transparency of the parallel variation highlight.
Defaults to \code{0.2}.}

\item{show_stats}{Logical. If \code{TRUE}, displays crossdating statistics (e.g., overlap, correlation, sgc, t-values)
as a plot subtitle. Defaults to \code{TRUE}.}
}
\value{
A \code{ggplot} object showing the plotted time series.
}
\description{
Creates a line plot of a dated tree-ring series and a reference chronology,
with optional z-score standardization, statistical annotations, and visual highlighting
of parallel variation (Gleichläufigkeit).
}
\details{
The function assumes that \code{x} and \code{y} are already calendar-dated and aligned by row names.
It performs internal trimming and optional z-scoring before plotting. Crossdating statistics
are computed using \code{trs_crossdate()}, and regions of synchronous growth change (SGC) are highlighted.
Required helper functions include: \code{\link{trs_trim}}, \code{\link{trs_zscore}},
\code{\link{trs_end_date}}, and \code{\link{sgc_for_plot}}. Statistics are derived from \code{\link{trs_crossdate}}.

Required packages: \strong{ggplot2}, \strong{ggtext}, \strong{plyr}
}
\examples{
x <- trs_pseudo_rwl(n_series = 1, series_length = 80, end_date = 1500, prefix = "trs_")
y <- trs_pseudo_rwl(n_series = 1, series_length = 400, end_date = 1700, prefix = "ref_")
trs_plot_dated(x, y)

}
