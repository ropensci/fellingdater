% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trs_plot_rwl.R
\name{trs_plot_rwl}
\alias{trs_plot_rwl}
\title{Plot all series in a tree-ring width dataframe}
\usage{
trs_plot_rwl(
  x,
  zscore = TRUE,
  facet = FALSE,
  ncol = 1,
  color = NULL,
  scale_y = "fixed"
)
}
\arguments{
\item{x}{A \code{data.frame} (typically of class \code{'rwl'}) with multiple columns of ring-width values.
Row names must be numeric and represent calendar years.}

\item{zscore}{Logical. If \code{TRUE}, the ring-width series are standardized to z-scores before plotting.
Defaults to \code{TRUE}.}

\item{facet}{Logical. If \code{TRUE}, creates a faceted plot with each series in a separate panel.
If \code{FALSE}, plots all series overlaid on the same plot. Defaults to \code{FALSE}.}

\item{ncol}{Numeric. Number of columns for faceted plots (only used when \code{facet = TRUE}).
Defaults to \code{1}.}

\item{color}{Character string or vector specifying line color(s). When \code{NULL} and \code{facet = FALSE},
each series gets a different color. When a single color is specified, all lines use that color.
When a vector of colors is provided, each series gets the corresponding color. Defaults to \code{NULL}.}

\item{scale_y}{Character string specifying y-axis scaling for faceted plots.
Options are "fixed", "free", "free_x", or "free_y". Defaults to \code{"fixed"}.}
}
\value{
A \code{ggplot} object showing the plotted time series.
}
\description{
Creates a line plot of all tree-ring series in an rwl dataframe,
with optional z-score standardization and faceting options.
}
\details{
The function assumes that the rwl dataframe is already calendar-dated with years as row names.
When \code{facet = FALSE}, all series are plotted on the same axes with different colors.
When \code{facet = TRUE}, each series gets its own panel for easier individual inspection.
When providing a vector of colors, the length must match the number of series in the data.
Required helper functions include: \code{\link{trs_trim}} and \code{\link{trs_zscore}}.

Required packages: \strong{ggplot2}, \strong{plyr}, \strong{tidyr}, \strong{dplyr}
}
\examples{
rwl_data <- trs_pseudo_rwl(n_series = 5, series_length = c(75, 100), end_date = c(2000, 2025))

# Overlaid plot with z-scores
trs_plot_rwl(rwl_data, zscore = TRUE, facet = FALSE)

# Faceted plot with raw values
trs_plot_rwl(rwl_data, zscore = FALSE, facet = TRUE, ncol = 1)

# Custom colors for each series
col_vec <- c("#0fa3b1", "#07beb8", "#3dccc7", "#68d8d6", "#9ceaef")
trs_plot_rwl(rwl_data, color = c("tomato3", "navyblue", "forestgreen", "purple", "orange"))
trs_plot_rwl(rwl_data, color = col_vec, zscore = FALSE)

}
